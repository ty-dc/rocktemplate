{{- if (eq .Values.templateController.tls.method "auto") }}
{{- $_ := include "generate-ca-certs" . }}
{{- end }}
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}
  annotations:
    {{- if (eq .Values.templateController.tls.method "certmanager") }}
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}-server-certs
    {{- end }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/mutate-template-crd-name"
        port: {{ .Values.templateController.webhookPort }}
      {{- if (eq .Values.templateController.tls.method "provided") }}
      caBundle: {{ .Values.templateController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.templateController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: template.spidernet.io
    rules:
      - apiGroups:
          - template.spidernet.io
        apiVersions:
          - v1
        operations:
          - CREATE
          - UPDATE
        #resources:
        #  - templateCrdName
    sideEffects: None

---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}
  annotations:
    {{- if (eq .Values.templateController.tls.method "certmanager") }}
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}-server-certs
    {{- end }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/validating-template-crd-name"
        port: {{ .Values.templateController.webhookPort }}
      {{- if (eq .Values.templateController.tls.method "provided") }}
      caBundle: {{ .Values.templateController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.templateController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: template.spidernet.io
    rules:
      - apiGroups:
          - template.spidernet.io
        apiVersions:
          - v1
        operations:
          - CREATE
          - UPDATE
          - DELETE
        #resources:
        #  - templateCrdName
    sideEffects: None

{{- if eq .Values.templateController.tls.method "certmanager" -}}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}-server-certs
  namespace: {{ .Release.Namespace }}
spec:
  issuerRef:
    name: {{ .Values.templateController.tls.certmanager.issuerName | trunc 63 | trimSuffix "-" }}
  secretName: {{ .Values.templateController.tls.secretName | trunc 63 | trimSuffix "-" }}
  commonName: {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc
  dnsNames:
    - {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}
    - {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}
    - {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc
    - {{ .Values.templateController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDnsDomain }}
  {{- range $dns := .Values.templateController.tls.certmanager.extraDnsNames }}
  - {{ $dns | quote }}
  {{- end }}
  {{- if .Values.templateController.tls.certmanager.extraIPAddresses }}
  ipAddresses:
  {{- range $ip := .Values.templateController.tls.certmanager.extraIPAddresses }}
  - {{ $ip | quote }}
  {{- end }}
  {{- end }}
  duration: {{ printf "%dh" (mul .Values.templateController.tls.certmanager.certValidityDuration 24) }}
  {{- end }}

{{- if (eq .Values.templateController.tls.method "provided") }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.templateController.tls.secretName | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  ca.crt:  {{ .Values.templateController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
  tls.crt: {{ .Values.templateController.tls.provided.tlsCert | required "missing tls.provided.tlsCert" }}
  tls.key: {{ .Values.templateController.tls.provided.tlsKey  | required "missing tls.provided.tlsKey"  }}
  {{- end }}

{{- if eq .Values.templateController.tls.method "auto" }}
---
{{- $cn := printf "spidernet.io" }}
{{- $ip := .Values.templateController.tls.auto.extraIpAddresses }}
{{- $dns1 := printf "%s.%s" .Values.templateController.name .Release.Namespace }}
{{- $dns2 := printf "%s.%s.svc" .Values.templateController.name .Release.Namespace }}
{{- $dns3 := printf "%s.%s.svc.%s" .Values.templateController.name .Release.Namespace .Values.global.clusterDnsDomain }}
{{- $dns := prepend .Values.templateController.tls.auto.extraDnsNames $dns1 }}
{{- $dns = prepend $dns $dns2 }}
{{- $dns = prepend $dns $dns3 }}
{{- $dns = prepend $dns $cn }}
{{- $cert := genSignedCert $cn $ip $dns (.Values.templateController.tls.auto.certExpiration | int) .ca }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.templateController.tls.secretName | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  ca.crt:  {{ .ca.Cert | b64enc }}
  tls.crt: {{ $cert.Cert | b64enc }}
  tls.key: {{ $cert.Key  | b64enc }}
{{- end }}